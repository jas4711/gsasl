Debian GNU/Linux build:
  tags:
    - glr1
  before_script:
    - apt-get update
    - apt-get install -y git autoconf automake libtool gettext cvs make
    - apt-get install -y texinfo texlive texlive-generic-recommended texlive-extra-utils texlive-font-utils
    - apt-get install -y help2man gtk-doc-tools valgrind
    - apt-get install -y dia libgnutls28-dev gengetopt gperf
    - make bootstrap CFGFLAGS="--enable-gcc-warnings --disable-silent-rules"
  script:
    - (make && make check) || (cat *.log test-suite.log; exit 1)
